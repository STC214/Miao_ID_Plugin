# TODO

## 已完成

1. [x] 更新本项目的文档。

2. [x] 给插件新增文件接收限制：
   - 只有私聊或临时对话窗口可以接收指定文件类型。
   - 指定文件类型暂定为 `har`、`zip`、`rar`。
   - 群聊内不直接接收或处理这类文件，避免敏感 HAR 泄露。

3. [x] 调整导入后的文件清理策略：
   - 压缩包导入、解析并成功写入有效设备模型后，自动删除原始压缩包。
   - 解压出来的文件长期保留，不自动清除。
   - 原本未压缩的裸文件长期保留，不自动清除。

4. [x] 修复审查风险：
   - 会话类型识别改为保守拒绝，无法明确判断为私聊/临时会话时不处理敏感文件。
   - URL 下载增加 30 秒超时和 10MB 大小上限。
   - 验证码日志旁路只在日志能关联到当前用户时提示，降低多人同时使用时的误报。

实现位置：

- `apps/mys-device.js`：导入入口限制为私聊或临时对话窗口。
- `lib/import-source.js`：记录待清理压缩包，URL 下载限制大小和超时，HAR 文件保留。
- `lib/verification-watch.js`：验证码日志提示收紧为用户相关日志才触发。
- `README.md`：补充临时直链服务示例和 URL 导入安全说明。
- `README.md`：同步更新使用说明和风险提示。
